# RAFIQ dependencies
# Standard-library only — no external packages are required (through phase 07).
# The Python Brain uses only the built-in `ast` module.

# Khwarizmi Core Architecture Foundation (Phase 1+)
torch>=2.0.0
numpy>=1.24.0
